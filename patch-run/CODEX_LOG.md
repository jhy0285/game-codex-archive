# Codex Collaboration Log

## 2026-08-13

- **Requested correction:** The premium characters still looked like static images sliding through the arena; make movement visibly clean and dynamic while explaining how browser character animation works.
- **Implementation:** Preserved the original generated character identities and Gate 1 simulation, but split the Curse Runner into mask, blade, paired mantle, and feet, and the Seed Husk into shell, paired wings, and six procedural root legs. Added player idle/walk/fire/dash compression-smear-recovery/hit/death states and enemy emerge/skitter/anticipate/hit/death states with clear transforms and follow-through.
- **Determinism and observability:** Animation is driven by the presentation clock, reported through `render_game_to_text`, and can be fixed to exact milliseconds through a debug-only capture hook. The browser test compares identical positions at fixed clocks and asserts animation frame changes and state transitions.
- **Visual review:** Opened and inspected same-position idle/walk pairs, fire recoil, dash smear, player hit, and enemy emerge/skitter/anticipate/hit/death captures. Increased mantle/feet gait amplitude, extended readable enemy hit/death poses, and moved the top inscription block so it no longer overlaps the title.
- **Verification and deployment:** Vitest 5/5 PASS; production build PASS; Playwright 3/3 PASS; required client PASS locally for two iterations and against production. Reused the linked Vercel project and updated `https://patch-run-weld.vercel.app`; root, `/pixel`, `/overdrive`, and actor assets return HTTP 200. Public state/canvas capture was inspected with animation telemetry present and no client error artifact.
- **Scope:** No Gate 2 rule, enemy archetype, route, collision geometry, simulation timing, or proprietary franchise asset was added. No human playtest is claimed.

## 2026-08-12

- **Requested correction:** Replace PATCH//RUN's still-obvious spaceship/radar premium skin with an original hand-painted gothic insect-fable direction while preserving every Gate 1 rule and route.
- **Implementation:** Generated and processed three original Drowned Scriptorium assets; made the root and `/overdrive` a flooded root-and-stone sanctuary with an ivory curse runner, bark seed husks, organic curse seeds, engraved serif UI, living-inscription patch rows, parchment/stone notices, three atmosphere planes, and batched organic impact splinters. `/pixel` and all patch behavior stayed unchanged.
- **Verification:** Vitest 5/5 PASS, production build PASS, Playwright 2/2 PASS, mandatory provided web-game client PASS for two action iterations, screenshots/state inspected for pre-patch combat, damage, inscription, growth ricochet, death, and route navigation. No human playtest claimed.
- **Deployment:** Reused the linked `ai-build3/patch-run` Vercel project; production alias `https://patch-run-weld.vercel.app` now serves the Drowned Scriptorium build. All routes and the three new assets return HTTP 200; a post-deploy provided-client run and visual/state inspection passed.

- **Requested task:** Study the linked Hollow Knight JavaScript project and raise PATCH//RUN to a much richer asset-led presentation while preserving its own design and Gate 1 contract.
- **Reference takeaways:** The inspected project separates environment, hero, enemy, particle, pause, audio, and UI assets; renders sprite animation through Canvas; and combines layered environments with camera shake, particles, fullscreen, and rapid state transitions. PATCH//RUN adopted those production principles, not its copyrighted art or characters.
- **Art direction and assets:** Used the built-in image-generation workflow to create an original painterly PATCH FORGE environment, ivory/cyan Patch Needle player craft, and bone/crimson Rupture Warden. The arena was cropped to the game aspect and compressed to a 237KB WebP. Sprite chroma keys were removed with the installed helper, alpha/corners were validated, and both sprites were padded and optimized to transparent 256px PNGs.
- **Implementation:** Made OVERDRIVE the canonical root build while retaining `/pixel`; added three low-object fog planes, forge-core pulse, batched engine/enemy glow, one-archetype arrival animation, and restrained dash/ricochet/kill shake. Added explicit Escape fullscreen exit. No Gate 2 mechanic or Parking Lot idea was implemented.
- **Problems found and fixed:** Initial reference-client review showed the licensed green/orange actor set clashed with the new forge material language, so it was replaced with the original cutouts. Expanded E2E found Escape did not exit fullscreen reliably under automation and that a bullet assertion ran after screenshot latency; explicit Escape handling and pre-capture state timing fixed both.
- **Tests performed:** Vitest; TypeScript/Vite build; provided web-game client after environment and actor passes; Playwright premium complete-loop smoke; build-selector navigation; fullscreen enter/exit; screenshot review for baseline combat, EVADE, hit, patch notice, grown ricochet, and death.
- **Results:** Vitest 5/5 PASS; production build PASS; Playwright 2/2 PASS and 6/6 sequential repeat PASS; provided client PASS; new routes/assets return HTTP 200; final inspected captures are readable and cohesive; no fatal page or console error was observed. No human playtest was claimed.
- **Requested task:** Review free itch.io game assets, apply suitable assets without over-restricting use, and fix the reported lag.
- **Implementation decisions made by Codex:** Used a dark seamless nebula from Screaming Brain Studios' itch.io CC0 pack, then used transparent player/enemy/laser/shield art and restrained combat audio from Kenney's official CC0 Space Shooter Remastered pack. Recorded provenance in `ASSET_CREDITS.md`. Preserved the one-enemy-archetype Gate 1 rules and the existing HUD hierarchy.
- **Performance problems found:** Every active bullet periodically created a new circle plus tween, the HUD re-rasterized text every frame, aim/enemy updates allocated temporary vectors, and each enemy recalculated a pulsing scale every frame. These costs multiplied under sustained fire and a full enemy cap.
- **Fixes:** Batched every projectile trail into one reused Graphics object, throttled HUD redraws to 10 Hz, reused/scalarized hot-loop math, removed enemy pulse scaling, used squared collision distance, iterated bullets without copying the group list, and capped active pursuers at 16. Added a sustained-fire render-object budget assertion to the browser smoke.
- **Tests performed:** Vitest; TypeScript/Vite production build; Playwright smoke; Playwright 3-run sequential stability check; provided web-game client movement/dash/aim/fire pass; imported-asset, EVADE, hit, patch-card, grown-ricochet, and death screenshot review; development-server asset checks.
- **Results:** 5/5 rule tests pass; build passes; Playwright smoke passes 3/3 sequential runs; CC0 assets return HTTP 200 from the live server; imported-asset captures are readable; no fatal browser or console error was observed. Machine verification does not replace the outstanding human feel/performance playtest.
- **Requested task:** Make the simple moving shapes modestly more attractive; show each installed rule prominently with an explanation; make hit and evade feedback unambiguous; consider additional fun elements.
- **Implementation decisions made by Codex:** Preserved the cold system-monitor direction while upgrading the player to a layered energy ship, the one enemy archetype to a rotating hunter drone, bullets to glow/trail projectiles, and the arena to a radar plane with slow scanline motion. Used cyan rings/afterimages/EVADE text for safe dashes and red border flash/impact shards/knockback/integrity text for damage. Enlarged patch cards and sourced their concise explanations from the rules module.
- **Technical problems found:** A bounced bullet's visual object was clamped inside the wall while its Arcade body remained outside, causing the ricochet to be destroyed on the next frame. Automated single-frame Space input could also be missed.
- **Fixes:** Reset the Arcade body inside the wall before applying reflected velocity; changed automation to hold Space; added deterministic feedback capture hooks without changing player-facing controls.
- **Tests performed:** Vitest; TypeScript/Vite build; provided web-game Playwright client; Playwright smoke; frame-by-frame text-state diagnosis; screenshots for combat, EVADE, hit, patch explanation, growth ricochet, and death.
- **Results:** 5 rule tests pass; build passes; Playwright smoke passes and its final stability run passes 3/3; provided-client combat passes with no fatal browser error. Machine screenshots were visually inspected. No human playtest was claimed.
- **Ideas considered but not implemented:** EVADE BONUS, PATCH CHAIN trick-shot multiplier, HOTFIX WINDOW risk zone, additional patches/enemies/arenas, and audio-reactive escalation were placed in GAME.md Parking Lot.

## 2026-08-11

- **Requested task:** Read `prompt_patch.txt`, create the required project documents, and ship a working PATCH//RUN Gate 1 prototype rather than a plan.
- **Implementation decisions made by Codex:** Used one Phaser arena scene, primitive generated textures, an immediate-play layout, a fixed deterministic patch scheduler, a compact state hook for automation, and isolated pure rule functions for Vitest. Normal shots always damage enemies; after a bounce they remain harmless to enemies until FRIENDLY FIRE activates. GROWTH multiplies a bounced bullet to 2.1× visual scale and 2.25× damage, with a gold trail and labeled wall impact. Canvas 2D was selected after headless WebGL capture proved unreliable for verification.
- **Technical problems found:** The repository contained only the default Vite demo and had no game, test scripts, project contract, or verification record. Integration checks then found Vitest/Playwright discovery overlap, a Phaser 4 tint API change, unrelated development servers on common ports, black WebGL screenshots, an unreliable post-death R edge, incorrect death-overlay bounds/HUD state, patch-notice overlap, a clipped wall label, and a nondeterministic ricochet capture under parallel browser load/random enemy interception.
- **Fixes:** Replaced the Vite demo; added the Phaser game and permanent patch interactions; added Vitest and Playwright coverage; isolated test discovery and port use; migrated tint handling; made browser captures deterministic; corrected restart/death UI; and created all requested documentation.
- **Tests performed:** `npm test`; `npm run build`; `npm run test:e2e`; provided web-game Playwright client with movement/dash/aim/shoot action bursts; text-state inspection; normal-combat, growth-ricochet, and death screenshot inspection; console/page-error checks; `git diff --check`.
- **Results:** Vitest PASS (4/4); production build PASS; Playwright smoke PASS (1/1) plus 3/3 sequential repeat runs; provided-client run PASS with an enemy kill and score change; no fatal browser error; screenshots visually readable after Canvas 2D change. Vite emits only a non-blocking Phaser bundle-size warning. No human playtest was claimed.
- **Human-provided game-design decisions:** Title PATCH//RUN; top-down arena action; WASD/mouse/left-click/Space controls; one pursuing enemy archetype; player health/damage/death/restart; patches every 20 seconds; exact RICOCHET, GROWTH, and FRIENDLY FIRE definitions; fixed activation order at 20/40/60 seconds; permanent stacking; no random selection in Gate 1; Gate 1 scope and pass criteria; technology and prohibited-feature constraints; milestone dates and deadline.
